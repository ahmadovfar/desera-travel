{
  "name": "desera-travel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ssg": "vite build && node scripts/prerender.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0",
    "vite": "^8.0.1"
  }
}
